# Copy this file to keystore.properties for signed release builds.
# Never commit the real keystore.properties file.

storeFile=release-keystore.jks
storePassword=change_me
keyAlias=release
keyPassword=change_me
